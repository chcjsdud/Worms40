#pragma once


enum class ActorGroup
{
	BackGround,
	Player,
	UI,
};

enum class PlayerState
{
	Idle,
	Move,
	Action
};

enum class RenderOrder
{
	BackGround,
	Weapon,
	Player,
	UI,
	Cursor,
};

enum class WeaponState
{
	Baz
};

// ∏ ≈∏¿‘
enum class MapType
{
	MAPBOOKS,
};